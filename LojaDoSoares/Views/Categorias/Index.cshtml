@model List<LojaDoSoares.Models.Categoria>
@{
    ViewData["Title"] = "Categorias";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1 class="my-4">Categorias</h1>
<a asp-asp-controller="Categorias" asp-action="Create" class="btn btn-primary mb-4">
    <i class="fas fa-add me-1"></i>Adicionar
</a>


<table class="table table-striped w-100" border="2">
    <thead>
        <tr>
            <th class="w-100">Nome</th>
            <th>Ações&nbsp;&nbsp;</th>

        </tr>
    </thead>


    </tbody>
    @foreach (Categoria categoria in Model)
    {
        <tr>nome</tr>
        <td>@categoria.Nome</td>
        <td>
            <a href="#" title="Alterar"><i class="fas fa-edit"></i></a>
            <a href="#" title="Detalhes"><i class="fas fa-search"></i></a>
            <a href="#" title="Excluir"><i class="fas fa-trash"></i></a>
        </td>
    }
    <tbody>
</table>